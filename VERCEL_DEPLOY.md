# 🚀 Настройки деплоя на Vercel

## Текущие настройки (правильные):

✅ **Framework Preset**: `Other` - правильно для статического сайта
✅ **Root Directory**: `./` - правильно, файлы в корне
✅ **Project Name**: `tggame` - можно оставить или изменить

## Что можно настроить (опционально):

### Environment Variables (переменные окружения)

Если хотите использовать токен бота в коде (хотя для статики это не обязательно):

1. Разверните секцию **"Environment Variables"**
2. Добавьте переменную:
   - **Name**: `BOT_TOKEN`
   - **Value**: `7986647026:AAEHPnmOg0gnwSG4J3pA-DAVUDjs5EGgnJc`
3. Выберите окружения: Production, Preview, Development

⚠️ **Важно**: Для статической игры (HTML/CSS/JS) переменные окружения не нужны, так как весь код выполняется в браузере. Токен можно использовать только на сервере.

### Build and Output Settings

Для статического сайта можно оставить по умолчанию:
- **Build Command**: не нужен (оставить пустым)
- **Output Directory**: не нужен (оставить пустым)

Vercel автоматически определит статический сайт и задеплоит его.

## После деплоя:

1. Скопируйте URL проекта (например: `https://tggame.vercel.app`)
2. Используйте его в BotFather как Web App URL
3. Проверьте работу игры

## Готово к деплою! 🎉

Можно нажимать кнопку **"Deploy"** - все настроено правильно!